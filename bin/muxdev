#!/usr/bin/env bash
# Developer wrapper — builds and runs the Go CLI from the repo root.
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec go run -C "$ROOT" ./cmd/muxdev "$@"
