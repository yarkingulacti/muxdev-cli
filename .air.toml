root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o /tmp/muxdev ./cmd/muxdev"
  entrypoint = ["/tmp/muxdev", "--version"]
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", "dist", ".git"]
  stop_on_error = true

[log]
  time = false
